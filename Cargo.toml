[package]
name = "video-diff-visualizer"
version = "0.1.0"
edition = "2024"

[dependencies]
image = "0.25"
ffmpeg-next = "9.0"
# ffmpeg-next = { version = "8.1.0", default-features = false, features = ["codec", "device", "filter", "format", "resampling", "software-scaling"] }
